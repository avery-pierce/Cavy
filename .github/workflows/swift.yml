name: Swift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:

    runs-on: macos-latest

    steps:
    - name: Xcode Test
      uses: sersoft-gmbh/xcodebuild-action@v1.3
      with:
        project: Cavy.xcodeproj
        scheme: Cavy
        action: test
